from django.apps import AppConfig


class MembersConfig(AppConfig):
    """Application configuration for the members app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "validibot.members"
